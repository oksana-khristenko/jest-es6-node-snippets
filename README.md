# Jest es6 node snippets

Spies, stubs, async, promises etc.

## Install

In terminal, run `npm i`.

## Test

In terminal, run `npm t` to test.

In terminal, run `npm run watch:test` to test and watch test files.